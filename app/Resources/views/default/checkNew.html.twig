{% extends 'layout.html.twig' %}

{% block content %}
    <h1>
        Neuer Check
        <small class="uk-text-muted uk-float-right">{{ checklist.name }}</small>
    </h1>
    {{ form(form) }}
{% endblock %}